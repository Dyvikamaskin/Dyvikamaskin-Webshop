/**
 * Login page — placeholder until Phase 3 (next-intl + UI components).
 * Auth logic lives in src/lib/auth.ts and src/lib/supabase/.
 */
export default function LoginPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Logg inn</h1>
      <p>Login UI implementeres i Phase 3.</p>
    </main>
  );
}
